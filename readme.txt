FIRST TIME SETUP

1.)open the terminal and run the command:
    npm install http-server --save

now you can launch the server by entering the command:
    npm start

Once the server is launched in the terminal, find the address it tells you it is available on, and copy and paste that into your browser URL to access the page